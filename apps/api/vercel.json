{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "cd ../.. && npm install",
  "buildCommand": "cd ../.. && npm run build -w @ventry/shared && cd apps/api && npx prisma generate",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
